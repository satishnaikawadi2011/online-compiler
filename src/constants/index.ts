import * as path from 'path';

export const SRC_PATH = `src`;
export const OUTPUT_PATH = path.join('src', 'outputs');
export const SOURCE_CODE_PATH = path.join('src', 'source-codes');

// For npm run dev command
// export const OUTPUT_PATH = path.join(process.cwd(), 'dist', 'src', 'outputs');
// export const SOURCE_CODE_PATH = path.join(process.cwd(), 'dist', 'src', 'source-codes');
// export const SRC_PATH = `${process.cwd()}/src`;
