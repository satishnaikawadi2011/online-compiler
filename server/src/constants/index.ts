import * as path from 'path';

export const SRC_PATH = `${process.cwd()}\\src`;
export const OUTPUT_PATH = path.join(process.cwd(), 'src', 'outputs');
export const SOURCE_CODE_PATH = path.join(process.cwd(), 'src', 'source-codes');
